import Control from "../../../common/control";

export class QuestionsHeader extends Control{
    constructor(parentNode: HTMLElement){
        super(parentNode, "div", "questions-header");

        
    }
}